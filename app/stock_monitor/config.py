"""
性能优化配置

参数说明：
    REQUEST_INTERVAL     : 每只股票采集之间的间隔（秒），防止请求过快触发限流
                           0 表示不间隔；推荐 0.1（N 只增加 N*0.1s，但显著降低封禁风险）
    DAILY_CACHE_ENABLED  : 是否启用日线数据缓存
                           日线数据日内不变（收盘后才更新），缓存后后续刷新无需重复请求
                           首次请求照常走网络，后续读缓存
"""

# 每只股票采集间隔（秒）
REQUEST_INTERVAL = 0.05

# 日线数据缓存（日内有效，避免重复请求）
DAILY_CACHE_ENABLED = True
